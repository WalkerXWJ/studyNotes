# vs code

## 1. 快捷键

显示所有命令: `shift` + `command` + `P`
转到文件：`command`+`P`
## 拉取git代码
### 图形化操作
1. 快捷键`shift` + `command` + `P`
2. 输入clone
3. 输入git链接，回车
## 插件

### Live Server

使用：右键index.html选择"Open with Live Server"

### Type Markdown Editer

一款所见即所得的即时渲染Markdown编辑插件（不支持即使保存）。
